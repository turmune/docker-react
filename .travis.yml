language: generic
sudo: required
services:
  - docker
  
before_install:
  - docker build -t turmune/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true turmune/docker-react npm run test